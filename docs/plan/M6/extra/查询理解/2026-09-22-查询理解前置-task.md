# 查询理解前置 — task（任务拆解）

> 对应 plan：`2026-09-22-查询理解前置-plan.md`
> 日期：2026-09-22 | 状态：已批准

## 任务列表

### T1: QueryUnderstanding record
- 新建 `src/main/java/com/agenticrag/intent/QueryUnderstanding.java`
- 四字段 record：`Intent intent`、`double confidence`、`String normalizedQuery`、`List<String> subQueries`
- 验证：`./mvnw compile -q` 通过

### T2: QueryUnderstandingService
- 新建 `src/main/java/com/agenticrag/intent/QueryUnderstandingService.java`
- 流程：开关判断（关闭→empty）→ 一次 LLM 调用（提示词含五类意图定义+规范查询要求+JSON 输出约束）→ 容错解析（去 ```` ```json ```` 包裹、trim）→ 校验（intent 枚举、confidence ≥ IntentProperties.confidenceThreshold）→ `Optional<QueryUnderstanding>` 返回
- 全路径 fail-open：LLM 异常 / 非法 JSON / 未知意图 / 低置信 → `Optional.empty()`，不抛异常
- 依赖：LlmClient、IntentProperties、RagProperties
- 验证：`./mvnw compile -q` 通过

### T3: 配置接入
- `RagProperties.Agent` 增 `queryUnderstandingEnabled`（默认 `true`），注释说明用途
- `application.yaml` 的 `rag.agent` 块增：
  `query-understanding-enabled: ${RAG_AGENT_QUERY_UNDERSTANDING_ENABLED:true}`
- 注意 yaml 缩进挂在 `rag.agent` 下（历史踩坑：`agent:` 块曾误入 `retrieval:` 块）
- 验证：`./mvnw compile -q` 通过；启动日志或断点确认配置绑定生效

### T4: ChatService.runAgent 接线
- 构建 AgentContext 前调用 `queryUnderstandingService.understand(userMessage)`
- 成功（非 empty）：
  - 在主 system 消息后追加一条「检索引导」system 消息：建议优先用 normalizedQuery（MULTI_TASK 附 subQueries 列表）调 search_knowledge_base；最终回答必须对齐用户原始问题
  - ChatResult.routedIntent 填入理解结果意图
- empty：完全走原逻辑（routedIntent 保持 null）
- 改动 ≤ 20 行；不碰 buildAgentMessages 原有结构、AgentLoop 零改动
- 验证：`./mvnw compile -q` 通过；`git diff --stat` 确认改动范围

### T5: 单元测试
- 新建 `src/test/java/com/agenticrag/intent/QueryUnderstandingServiceTest.java`，六类路径：
  1. KB_QA 正常产出（intent + normalizedQuery）
  2. MULTI_TASK 正常产出（附 subQueries 列表）
  3. 非法 JSON → empty
  4. 未知意图（不在五类枚举）→ empty
  5. LLM 调用抛异常 → empty
  6. 开关关闭 → empty 且 LlmClient 零调用（verifyNoInteractions）
- 验证：`./mvnw test -Dtest=QueryUnderstandingServiceTest` 全绿

### T6: 全量回归
- `./mvnw test` 全量测试无回归（当前基线 135 例全绿）
- 验证：BUILD SUCCESS，无既有测试失败

### T7: r09 原题消融
- 命令：
  ```bash
  cd /Users/miaobao/agent-projects/agentic-rag && \
  EVAL_ERAG_CHAT_MODE=agent RAG_AGENT_DOCUMENT_TOOLS_ENABLED=false \
  ROUNDS_DIR=eval-answers/rounds-era2 \
  ./scripts/run-erag-round.sh r09 hybrid 1 1 1 > eval-answers/rounds-era2/r09-run.log 2>&1
  ```
- 作答完成后判分：
  ```bash
  cd /Users/miaobao/agent-projects/agentic-rag && \
  ROUNDS_DIR=eval-answers/rounds-era2 \
  ./scripts/run-erag-judge.sh r09 > eval-answers/rounds-era2/r09-judge.log 2>&1
  ```
- 口径核对（对照 r07d 唯一变量）：AGENT + hybrid + 改写开 + HyDE 开 + rerank 开 + citationTopN=5 + 文档工具开关关闭 + **查询理解开（唯一变量）**
- 验证：`eval-answers/rounds-era2/r09.jsonl` 行数 = 64；run 日志出现查询理解调用日志；answers 无 `<tool_call>` 泄漏；判分四指标生成

### T8: r09v1 变体消融 + 归档
- 命令（变体题库 `eval-answers/rounds-era2/v1-dejargon.jsonl`，62/64 英文）：
  ```bash
  cd /Users/miaobao/agent-projects/agentic-rag && \
  EVAL_ERAG_CHAT_MODE=agent RAG_AGENT_DOCUMENT_TOOLS_ENABLED=false \
  EVAL_ERAG_QUESTIONS=eval-answers/rounds-era2/v1-dejargon.jsonl \
  ROUNDS_DIR=eval-answers/rounds-era2 \
  ./scripts/run-erag-round.sh r09v1 hybrid 1 1 1 > eval-answers/rounds-era2/r09v1-run.log 2>&1
  ```
  判分同上（轮名 r09v1）。
- checklist 回填 r09 × r07d、r09v1 × v1 基线（40.23）对比表与归因结论
- commit + push 三分支（feature → main/dev）
- 验证：对比表归档；远端分支同步

## 依赖关系

T1 → T2 → T3 → T4 → T5 → T6 顺序执行；T7 → T8 串行（同一评测库，避免并发写）。

## 判定标准（写入 checklist）

- **原题防回退**：r09 corr 相对 r07d（43.75）波动在 ±3pp 内视为无回退。
- **v1 达标线**：r09v1 recall 相对 r09 原题 recall 的差距 < 15pp（v1 基线差距为 -28.1pp）。
- 两条件同时满足 → 固化默认开启；仅 v1 达标但原题回退 → 保留开关、默认关闭，报告中说明。

## 环境教训（沿用 r08b）

- Bash 工具 cwd 在多次调用间不保持：**每条命令必须自带 cd**（后台任务尤其）。
- GitHub 推送经代理间歇 502：带重试循环推送三分支。
